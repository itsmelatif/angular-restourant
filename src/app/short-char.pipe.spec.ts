import { ShortCharPipe } from './short-char.pipe';

describe('ShortCharPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortCharPipe();
    expect(pipe).toBeTruthy();
  });
});
